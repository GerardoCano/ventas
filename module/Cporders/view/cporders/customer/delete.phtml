<?php
// module/Album/view/album/album/delete.phtml:

$title = 'Delete Customer';
$url   = $this->url('customer', ['action' => 'delete', 'id' => $id]);

$this->headTitle($title);
?>
<h1><?= $this->escapeHtml($title) ?></h1>

<p>
    Are you sure that you want to delete to customer 
    "<?= $this->escapeHtml($customer->first_name) ?>  
    <?= $this->escapeHtml($customer->last_name) ?>"?
</p>

<form action="<?= $url ?>" method="post">
<div class="form-group">
    <input type="hidden" name="id" value="<?= (int) $customer->id ?>" />
    <input type="submit" class="btn btn-danger" name="del" value="Yes" />
    <input type="submit" class="btn btn-success" name="del" value="No" />
</div>
</form>