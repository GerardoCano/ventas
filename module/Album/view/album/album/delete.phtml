<?php

$title = 'Eliminar álbum';
$url   = $this->url('album', ['action' => 'delete', 'id' => $id]);

$this->headTitle($title);
?>

<!-- Titulo de la sección -->
<h1><?= $this->escapeHtml($title) ?></h1>

<!-- Mensaje de confirmación para eliminar el registro -->
<p>
    ¿Está seguro que desea eliminar el album
    "<?= $this->escapeHtml($album->title) ?>" de
    "<?= $this->escapeHtml($album->artist) ?>"?
</p>

<!-- Botónes para aceptar o declinar al acción -->
<form action="<?= $url ?>" method="post">
<div class="form-group">
    <input type="hidden" name="id" value="<?= (int) $album->id ?>" />
    <input type="submit" class="btn btn-danger" name="del" value="Si" />
    <input type="submit" class="btn btn-success" name="del" value="No" />
</div>
</form>